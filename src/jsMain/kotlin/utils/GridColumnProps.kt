package utils

import mui.material.GridProps

var GridProps.xs: Int?
    get() = asDynamic().xs
    set(value) {
        asDynamic().xs = value
    }
var GridProps.md: Int?
    get() = asDynamic().md
    set(value) {
        asDynamic().md = value
    }

var GridProps.sm: Int?
    get() = asDynamic().sm
    set(value) {
        asDynamic().sm = value
    }

var GridProps.lg: Int?
    get() = asDynamic().lg
    set(value) {
        asDynamic().lg = value
    }

var GridProps.xl: Int?
    get() = asDynamic().xl
    set(value) {
        asDynamic().xl = value
    }